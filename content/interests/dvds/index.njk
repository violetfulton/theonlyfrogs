---
title: "Physical DVD Collection"
layout: layouts/frogCollectionBase.njk
customcss: "/assets/css/dvds.css"
permalink: /dvds/
---

<h1>🎬 My Physical DVD Collection</h1>

<div class="filter-controls">
  <button class="filter-btn active" data-filter="all">All</button>
  <button class="filter-btn" data-filter="watched">Watched ✅</button>
  <button class="filter-btn" data-filter="unwatched">Unwatched 🎯</button>
</div>

<div class="watch-progress">
  <div class="progress-bar">
    <div class="progress-fill"></div>
  </div>
  <p id="progress-text"></p>
</div>

<div class="movie-grid">
{% for movie in movieCollectionPhysical %}
  {% set poster = movie.poster_path | default("/assets/imgs/frog-dvd-placeholder.png") %}
  {% set releaseYear = movie.release_date and movie.release_date.slice(0,4) %}

  <div class="movie-card"
       data-title="{{ movie.title }}"
       data-year="{{ releaseYear }}"
       data-img="https://image.tmdb.org/t/p/w342{{ poster }}"
       data-my-rating="{{ movie.my_rating }}"
       data-status="{{ movie.status }}">

    <img src="https://image.tmdb.org/t/p/w342{{ poster }}" alt="{{ movie.title }}">
    <p class="title">{{ movie.title }}</p>
  </div>
{% endfor %}
</div> <!-- end movie-grid -->

{% include "partials/dvdModal.njk" %}
</main>

<script src="/assets/js/dvdModal.js?v=11"></script>
