---
title: "🎮 Missing Game Covers"
layout: layouts/gamesBase.njk
permalink: /games/missing/
customcss: '/assets/css/games.css'
---

<h1>🎮 Missing Game Covers</h1>
<p>These games don't have cover art yet!</p>

{% set missing = gamesMissing %}

{% if not missing or missing.length == 0 %}
<p>✅ All games have covers — amazing job!</p>
{% else %}
<p>📦 Missing covers: <strong>{{ missing.length }}</strong></p>
<ul class="missing-covers-list">
{% for game in missing %}
  <li>
    <strong>{{ game.title }}</strong> — {{ game.platform }}

    <button class="copy-btn"
      data-filename="{{ game.slug }}.jpg">
      📎 Copy
    </button>
  </li>
{% endfor %}
</ul>
{% endif %}

<script>
document.addEventListener("click", function(e) {
  if (e.target.matches(".copy-btn")) {
    const filename = e.target.dataset.filename;
    navigator.clipboard.writeText(filename).then(() => {
      e.target.textContent = "✅ Copied!";
      setTimeout(() => (e.target.textContent = "📎 Copy"), 1200);
    });
  }
});
</script>
