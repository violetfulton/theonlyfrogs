---
layout: cdsBase.njk
title: CD Collection
permalink: /interests/cds/test/
customcss: '/assets/css/cdsTest.css'
---

<h1>{{ title }}</h1>

{% if releases.length %}
<div class="cd-grid">
  {% for cd in releases %}
    <div class="cd-card">
      <img src="{{ cd.image }}" alt="{{ cd.title }}">
      <div class="cd-overlay">{{ cd.title }}</div>
      <p class="cd-info">{{ cd.artist }} - {{ cd.format }} - {{ cd.year }}</p>
    </div>
  {% endfor %}
</div>
{% else %}
<p>No CDs found. Check your Discogs API setup or token.</p>
{% endif %}

