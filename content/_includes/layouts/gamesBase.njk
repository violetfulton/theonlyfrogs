<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title or pageTitle or "The Only Frogs - Game Collection" }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="/assets/img/frog-icon.png">

  {# shared CSS for all pages #}
<link rel="stylesheet" href="/assets/css/games/shared.css">

{# page-specific CSS (string or array) #}
{% if customcss %}
  {% if customcss | length and (customcss is not string) %}
    {% for href in customcss %}
      <link rel="stylesheet" href="{{ href }}">
    {% endfor %}
  {% else %}
    <link rel="stylesheet" href="{{ customcss }}">
  {% endif %}
{% endif %}

</head>
<body class="games-shell">

  <header class="games-header">
    <div class="games-header-inner">
      <a class="games-brand" href="/games/" aria-label="Game Collection home">
        <img class="games-brand-img" src="/assets/imgs/frog-controller.png" alt="Frog with Game Controller">
      </a>

      <div class="games-title-wrap">
        <h1 class="games-title">{{ title or "Game Collection" }}</h1>
        <p class="games-subtitle">insert coin • press start • browse the shelf</p>
      </div>
    </div>
  </header>

  <main class="games-page {{ pageClass or '' }}">
    <div class="games-frame">
      {{ content | safe }}
    </div>
  </main>

  <footer class="games-footer">
    <div class="games-footer-inner">
      <hr class="games-divider">

      <marquee class="games-marquee" behavior="scroll" direction="left" scrollamount="3">
        🎮 Hop into my video game collection! 🎮
      </marquee>

      <p class="games-footer-links">
        return <a class="games-link" href="/">home</a> 🐸
      </p>
    </div>
  </footer>

</body>
</html>
